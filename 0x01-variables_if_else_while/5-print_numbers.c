#include <stdio.h>
/**
 * main - Entry point
 * program to print numbers 0-9
 * Return: 0 (Success)
 */
int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
		printf("%d", a);
	printf("\n");
	return (0);
}
